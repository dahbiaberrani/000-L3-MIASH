
#include <vector>

std::vector<int> un_tableau_dynamique;

#include <list>

std::list<int> une_liste_doublement_chainee;

#include <forward_list>

std::forward_list<int> une_liste_simplement_chainee;

#include <deque>

std::deque<int> un_tableau_a_deux_extremites;

std::priority_queue<int> une_file_avec_gestion_de_priorité;

#include <map>

std::map<int, double> un_tableau_associatif_clés_triées;

std::multimap<int, double> un_tableau_associatif_clés_triées_avec_collision;

#include <set>

std::set<int> un_ensemble_sans_doublon;

std::multiset<int> un_ensemble_avec_collision;

#include <unordered_map>

std::unordered_map<int, double>  un_tableau_associatif_clés_hashées;

std::unordered_multimap<int, double> un_tableau_associatif_clés_hashées_avec_collision;

#include <unordered_set>

std::unordered_set<int> un_ensemble_sans_doublon;

std::unordered_set<int> un_ensemble_avec_collision;

#include <stack>

std::stack<int> une_pile;

#include <queue>

std::queue<int> une_file;

auto main() -> int {

	

	return 0;
}